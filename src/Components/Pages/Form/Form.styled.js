import { styled } from '@mui/material';

export const SteperForm = styled('form')(() => ({
  width: '100%',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  marginTop: '60px',
}))