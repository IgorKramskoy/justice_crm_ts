import {
    Box,
    styled
} from '@mui/material';

export const BoxImage = styled(Box)(() => ({
    width: '24px',
    height: '24px',

}));
export const BoxText = styled(Box)(() => ({
    alignItems: 'center',
    fontSize: '14px',
}));
export const StylesBox = styled(Box)(() => ({
    display: 'flex',
    gap: '10px',
    alignItems: 'center',
    padding: '10px',
}));